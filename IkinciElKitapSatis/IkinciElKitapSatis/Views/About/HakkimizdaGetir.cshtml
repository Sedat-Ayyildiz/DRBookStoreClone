@model IkinciElKitapSatis.Models.HAKKIMIZDA

@{
    ViewBag.Title = "HakkimizdaGetir";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<body class="container">
    <div id="layoutSidenav_content" class="mt-3">
        <main>
            <div class="container px-4">
                <div class="card mb-4">
                    <div class="card-header">
                        <i class="fas bi-book me-1"></i>
                        <span class="fs-3">Hakkımızda Güncelleme Sayfası</span>
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm("HakkimizdaGuncelle", "About", FormMethod.Post))
                        {
                            @Html.HiddenFor(s => s.HAKKIMIZDAID)
                            @Html.LabelFor(s => s.HAKKIMIZDAACIKLAMA)
                            @Html.TextBoxFor(s => s.HAKKIMIZDAACIKLAMA, new { @class = "form-control" })
                            <br />
                            @Html.LabelFor(s => s.SPONSORRESIM)
                            @Html.TextBoxFor(s => s.SPONSORRESIM, new { @class = "form-control" })
                            <br />
                            @Html.LabelFor(s => s.SPONSORISIM)
                            @Html.TextBoxFor(s => s.SPONSORISIM, new { @class = "form-control" })
                            <br />
                            <button class="btn btn-success">Güncelle</button>
                            <a href="/Author/Index" class="btn btn-outline-danger">Vazgeç</a>
                        }
                    </div>
                </div>
            </div>
        </main>
        <!--FOOTER-->
        @Html.Partial("_PartialFooter")
    </div>
</body>